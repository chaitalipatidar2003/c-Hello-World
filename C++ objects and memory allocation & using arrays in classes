#include <iostream>
using namespace std;

class
{
    int itemId[100];
    int itemPrice[100];
    int counter;

public:
    voidinitCounter { counter = 0; }
    void setPrice(void);
    void displayPrice(void);
};

void shop ::setPrice(void)
{
    cout << "Enter Id of your item no " << [counter] << endl;
    cin >> itemId[counter];
    cout << "Enter price of your item " << endl;
    cin >> itemPrice[couter];
    counter++;
}
void shop ::displayPrice(void)
{
    for (int i = 0; i < counter; i++)
    {
        cout << "The price of item with Id " << itemId[i] << " is " << itemPrice[i] << endl;
    }
}

int main()
{
    shop dukaan;
    dukaan.initcounter();
    dukaan.setPrice();
    dukaan.setPrice();
    dukaan.setPrice();
    dukaan.displayPrice();

    return 0;
}
